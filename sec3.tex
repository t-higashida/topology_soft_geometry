\chapter{2次元のトポロジー}

\section{閉曲面}

前章では1次元の図形，すなわち曲線について考察し，そのつながり方をベッチ数，
切断数，オイラー標数などで調べてきました．そのとき私達が用いた方法は，
曲線を有限個の線分でできた図形（これを1次元複体と呼びました）に置き替えて考えよう，
ということでした．このような置き替えをすることによって，たとえば，ジョルダンの曲線定理の証明
などは非常にすっきりとまとまったものになったのです．

\問題{3.1}
ジョルダンの曲線定理を一般の滑らかな曲線に拡張するとき，前の証明のどこが問題となるでしょうか．

\解

直線の方向をどのように選んでも，その方向の直線が閉曲線$C$と次の形で接することがあり，
このとき，交点の個数が意味を持たなくなる．


\■

一般の曲線の中には上のように$1$次元複体で置き替えられない怪物も存在しますが，
ここではそれらは考えに入れないで，$1$次元複体で置き替えられるものだけを扱ってきたわけで
す．

\例{}

次の曲線は1次元複体で置き替えることはできません．

(1)ペアノ曲線

図3.1の操作を無限回くり返すと正方形の内部を埋めつくす閉曲線が得られます．これを
ペアノ曲線といい，G．ペアノ（1858～1932，イタリア）によって初めて構成されました．
ぺ

図3.1

ペアノ曲線は，当然のことながら，1次元複体にはなりません．


(2) 一つの円周上に無限に小さくなっていくように結び目を並べたもの，を考えます．
一般にこのような結び目を野性の結び目といいますが，これも$1$次元複体とはならない曲線の
例となります（図3.2）．

図3.2



つまり私達が考えてきた図形はこのような野性の図形を含まない育ちの良いものだったわ
けです．これらの例を踏まえて，曲面にあたるものを第2章と同じ立場で定義していきます．

\定義{3.13}
多角形（周を含む）を$2$次元単体といい，$3$個の頂点$x,y,z$で決まる$2$次元単体を
$|xyz|$と書きます．また，$|xyz|$からその頂点と周を取り除いた図形を$2$次元単体の内部また
は開いた$2$次元単体といいます．これからはそれぞれ$2-$単体，開$2-$単体と呼びます
（図3.3）．さらに$|xyz|$の頂点と周を合わせたものを$|xyz|$の境界といい，$\text{bd}$と書きます
（図3.4）．$3$角形の形状はどうでもよいことに注意しておきましょう．ただし，つぶれて線分とな
っているものは$2-$単体とは呼びません．$|xyz|$の周を形作っている$0-$単体，$1-$単体および自
分自身を$|xyz|$の面といいます．

\例{}

図3.3

図3.4

\定義{3.2}
有限個の$0,1,2-$単体の集合$K$が次の条件をみたすとき$K$を$2$次元複体といいます．
\begin{list}{}{}
	\item	(1) $|xyz|$が$K$の$2-$単体のとき，その面は$K$の単体になっている．
	\item (2) $K$の2個の単体が交わるとすれぼ，それらは共通の面で，ぴったりと交わる．
\end{list}

例

（図3.5）

例のように$4$面体の表面などは$2$次元複体となり，これは柔らかい視点で見て，
膨らませれば，球面になることが分かります．したがって$4$面体の表面は“曲面”と見なしてもいいでしょう．
しかし他の二つの例は曲面と呼ぶのがためらわれます．そのために$2$次元複体の複体とならないものの


(2) の粂件をみたさない．



4面体の表面


図3.5



うちで姿・形のいいものだけを選び出して，それを曲面と呼ぶことにします．

\定義{3.3}

$ |xyz|$を$2-$単体とします．$0-$単体$x$に対して$1-$単体$|yz|$を$x$の対面といいます．
（図3.6）．
逆に$1-$単体$|yz|$に対して$0-$単体$x$を$|yz|$の対面といいます．また$1-$単体$|xy$につ
いては$y$を$x$の対面といいます．

すなわち$2-$単体$lxyz|$は対面どうしをつなぎ合わせて作られていると見なせるわけです．


$K$の対面

図3.6


さて，これを用いて複体$K$の各$0-$単体$x$に対してリンクというものを定義します．

\定義{3.4}

$2$次元複体$K$の$0-$単体を$x$とします．$x$を頂点に持つ単体でのxの対面すべて
を集めたものを$K$の$K$におけるリンクと呼んで，
$	\mathit{lk} ( x ; K)$書きます．
すなわち
\[
	\mathit{lk} (x ; K)
  =
  \{	\Delta \* | \: \text{$\Delta$は$K$の単体で$x$と$\Delta$は$K$のある単体の中で互いに対面となっている}
	\}
\]

また$x$を面に持つ$K$の単体およびその面すべてを集めたものを，
$x$の$K$におけるスターと呼んで，$\mathit{st} (x;K)$と書きます．
すなわち
\[
	\mathit{st} (x : K) 
  =
  \{ \Delta | \text{$\Delta$は$K$の単体で$x$を頂点にもつ単体の面となっている}
  \}
\]

\例{}

（図3.7）

図3.7


以上の準備の下で，”曲面”を次のように定義します．

\定義{3.5} $2$次元複体$K$が次の条件をみたすとき$K$を閉曲面といいます．

(1) $K$の各$0-$単体xについて$ \mathit{lk} ( x; K) $はただ$1$個の閉多角形となる．

\例{}

閉曲面とならない$2$次元複体（図3.8），

閉曲面となる2次元複体（図3.9）．


図3.8



4面体の表面

ik(x;K)=

四角いトーラス

図3.9



ここで1次元複体のときと同様に，次のようにして連結の定義をしておきます．

\定義{3.6}閉曲面$K$の任意の2個の$0-$単体を$x,y$とします．
$x,y$を結ぶ$K$の$1-$単体の列が存在するとき，
$K$は連結であるといいます．また閉曲面$K$が
\[
	K = K_1 \cup K_2 \cup \cdots \cup K_r , \quad K_i \cap K_j = \varnothing \quad (i \ne j)
\]
かつ各Kiは連結な閉曲面，と分解されるとき，各$K_i$を$K$の連結成分といい，Kの連結成分
の個数$r$を閉曲面$K$の$0$次元ベッチ数といって$ p_0 (K) $と書きます．

これからの話では，扱いを簡単にするため，
\[
	p_0 (K) = 1
\]
である複体だけに対象を限定しましょう．

さて，前の例で球面・トーラスなどがそれぞれ4面体の表面・箱形トーラスとして閉曲面
になることを見ましたが，これは$4$面体の各頂点のリンクを調べて分かったことでした．
しかし4面体の表面はさらに細かく3角形に分けられます．そのようなときでもリンクはきち
んと閉多角形になっているでしょうか．

例(図3.10).


図3.10

上の例で見るように，確かに$x$のリンクは閉多角形となっています．これをきちんと示す
ために幾つかの準備をしましょう．

\定義{3.7} $K,L$を2次元複体とします．Kの$0-$単体に$L$の$O-$単体を対応させる写像
$ f: K \to L$について，$| xyz|$ が $K$の$2$-単体なら$ | f(x) f(y) f(z) |$が
$L$の単体 ($2$-単体でなくてもかまいません），
また$|xy|$が$K$の$1-$単体なら $ | f(x)f(y) |$ が$L$の単体となるとき，
$f$を単体写像といいます．$ f : K  \to L$ なる単体写像が全単射のとき
$f$を単体同型写像，また$K$と$L$とは同型であるといって
\[
	K \simeq L
\]
と書きます．第2章の定義を思い出してください．これらの定義は第4章で一般の形で考察
することになります．

さて，
\[
	K \simeq L
\]
かつ$K$が閉曲面なら，$L$も閉曲面であることは明らかでしょう．

\問題{3.2}
このことを単体同型写像 $f : K  \to L$を用いてきちんと証明してください．

\証明

$f : K \to L$を単体同型写像とする．$L$の任意の点を$y$とし，
$f(x)  = y$となる$K$の頂点$x$をとる．
$ \mathit{ lk} ( x; K )$は閉多角形，かつ，$f$を$[mathit{lk} (x;K)$に制限したのは，
\[
	\mathit{lk} (x; K ) \simeq f( \mathit{lk} ( x ; K))
\]
なる単体同型を与える．ここで
\[
	\mathit{lk} (y;L ) = f( \mathit{lk} ( x ; K))
\]
を示せば，$\mathit{lk} (y;L)$も閉多角形となる．

\証明

$	\mathit{lk} (y ; L) \ni \Delta$とすれば，$y * \Delta$は$L$の単体であるから，
\[
	f(x * \Delta') = y * \Delta
\]
となる$K$の単体$\Delta'$があり，
\[
	f ( \Delta ') = \Delta
\]
である．
\[
	\therefore \Delta \in f( \mathit{lk} ( x; K)).
\]
逆に$	f( \mathit{lk} ( x; K)) \ni \Delta$とすれば，
\[
	f ( \Delta') = \Delta , \quad \Delta \in \mathit{lk} ( x; K)
\]
となる$ \Delta '$があり，$ x * \Delta'$は$K$の単体である．よって
\[
	f ( x * \Delta') = f(x) * f ( \Delta ' ) = y * \Delta.
\]
は$L$の単体となり，$\Delta \in \mathit{lk} ( y ; L)$である．


\定義{3.8}
$K$を$2$次元複体とします．$K$の$1-$単体，$2-$単体の中に新しく幾つかの$0-$単体を
とり，$K$を細かく分割した複体を$K$の細分といい，$K'$と書きます．

\例{}

(図3.11)

$K$の細分の例

図3.11

これらを用いて2次元複体に柔らかい視点を持ちこみます.

定義3.9K,Lを2個の2次元複体とします.Kの細分K'とLの細分L'を適当に選んで
\[
	K' \simeq L'
\]
とできるとき，$K$と$L$とは組合せ同相であるといい，
\[
	K \approx L
\]
と書きます．

これが2次元複体についての柔らかい視点で，これからは組合せ同相な2個の
複体は同じものと見なすという立場をとります．これも1次元の場合と同じく，
「ぐにゃぐにゃ」というよりは「ポキポキ」という感じですが，私達の扱っている図形が複体という
「角ばった」図形ですので，このほうが都合がいいのです．

\例{}

(図3.12)

図3.12

数字どうしが対応

\定理{3.1}
$K \approx L$かつ$K$が閉曲面なら$L$も閉曲面である．

この定理によって，2次元複体が閉曲面かどうかは組合せ同相で，すなわち柔らかい視点
で，不変な性質であることが分かります．定理の証明には，幾つかの補助定理を用います．

\textbf{補助定理}


$K$を2次元複体とし，$K'$を$K$の細分とする．
$K$の$0$-単体を$x$とすると，
\[
	\mathit{lk} ( x ; K ) \approx \mathit{lk} ( x ; K').
\]
である．

\証明

$\mathit{lk} (x;K')$の任意の$0-$単体を$y'$とする．$y'$を内部に含む$K$の単体が1個あるが,
この単体は1次元または2次元であるとしてよい（図3.13）．

は細分した$K'$
図3.13


(1) $y'$が1-単体$|xy|$に入るとき．$y'$に$x$の対面$y$を対応させる．

(2) $y'$が2-単体$|xwz|$に入るとき．半直線 $ \overline{xy'}$が$x$の対面$|wz|$と交わる点を$y$とし，
対面$|wz|$を$|wy|$と$|yz|$に細分する．この細分で$ \mathit{lk} (x;K)$ は$ \mathit{lk} (x;K')$と同型になる．

(3) $1$-単体$|xy|$が3個以上の$2-$単体の共通の面となっているとき。

その$3$個の$2$単体$|xyu|, | xyv|, | xyw|$とするとき，$ |yu|, |yv|, |yw|$はすべて
$ \mathit{lk} (x ; K)$に入る．したがって$\mathit{lk} (x;K)$内$ \deg y$は$3$以上となり，
$ \mathit{lk} (x , K)$は閉多角形とならない．

(1)(2)(3)より，$K$が閉曲面のとき各単体はちょうど3個の$2$-単体の共通の面となる．

\■

\textbf{補助定理}

$K$を閉曲面，$K'$を$K$の細分とする．$K$に新しく付け加わった$0$-単体を$x'$とするとき、
$\mathit{lk}  x' + K')$は閉多角形である．

\証明

$K'$に新しく付け加わる$0$-単体は，$1$-単体または$2$-単体上にある．

(1) $x'$が$K$の$1$-単体上にあるとき，

補助定理により，$|uv|$を面に持つ$K$$2$-単体は$|uvz|, | uvz|$とちょうど2個ある．
$\mathit{lk} (x' ; K')$より
$0$-単体$y'$をとると，$y'$は$|uvw| \cup |uvz|$上にある．半直線$\overline{x'y'}$が
\[
	L = | uz| \cup |zv| \cup |vw| \cup |wu|
\]
と交わる点を$y$とし，$y$を新しい$0$-単体として$L$を細分する．
逆に，$L$上の$0$-単体$y$に対して線分$ \overline{x'y}$が$\mathit{lk} ( x' ; K)$と交わる点を
$y'$とし，$y'$を新しい$0$-単体として，$ \mathit{lk} ( x' ; K')$を細分する．

これらの細分によって$L$と$ \mathit{lk} ( x' ; K')$とは組合わせ同型となるから，
\[
	\mathit{lk} ( x' ; K') \approx L
\]
かつ$L$は明らかに閉多角形である．

(2) $xx'$が$K$の2単体$ |uvw| $上にあるとき

$ \mathit{lk} ( x' ; K')$の任意の頂点$y'$と$x'$を結ぶ半直線
$| x' y'|$が$ | uvw |$の境界
$ \text{bd} | uvw |$と交わる点を$y$，また$ \overline{ux'} , \overline{vx'} , \overline{ wx'} $が交わる点を
$ u' , v' , w'$として$ \mathit{lk} ( x' K')$および$ \text{bd} | uvw |$を細分すれば
\[
	\mathit{lk} (x' ; K') \simeq \text{bd} | uvw | '
\]
となるが，右辺は明らかに閉多角形である．

\■

以上の補助定理によって前の定理が証明されたことになる．



では，細分によって新しく付け加わる0-単体のリンクはどうなるでしょうか．
これについては$ K $が閉曲面のときについて調べて見ましょう．

\textbf{補助定理}

$K$を閉曲面とする．$K$の任意の$1$-単体はちょうど2個の$2$-単体の共通の面で
ある.

\証明

背理法で行なう.

図3.14


(1) $1$-単体lxylがどの$2$-単体の面にもならないとき（図3.14の(1)）.
このとき$y$は$ \mathit{lk} (x;K)$ 内に孤立した$0$-単体として現われる他なく，
$\mathit{lk} (x;K) $が閉多角形とならない．

(2) $1$-単体$|xy|$がただ1個の$2$-単体$|xyz|$の面となっているとき，

（図3.14の(2) ）．


$|ya|$は $ \mathit{lk} ( x; K ) $に入る$1$-単体であるが，$y$に面をもつ
$ |yz| $以外の$1-$単体は，$\mathit{lk} (x;K) $に入らないから，
$ \mathit{lk} (x;K)$ 内 $\ deg y $は$1$となり，$ \mathit{lk} (x;K) $は閉多角形とならない．


